[advice]
[PY-AGENT-R009] Info: Function hides algorithm behind nested control flow
  --> $TEMP/quality.py:4:1
   4 | def nested(enabled: bool, ready: bool, valid: bool, active: bool) -> int:
     | `- make this algorithm shape explicit
     | Required: Flatten deeply nested if/loop logic into guard clauses, explicit dispatch, match/case, or small named pipeline steps so agents can reason about the algorithm shape. Signals: deep control-flow nesting.
     | Signals: agent-coding-quality/control-flow.decision-stack

[PY-AGENT-R009] Info: Function hides algorithm behind nested control flow
  --> $TEMP/quality.py:13:1
  13 | def route(kind: str) -> int:
     | `- make this algorithm shape explicit
     | Required: Flatten deeply nested if/loop logic into guard clauses, explicit dispatch, match/case, or small named pipeline steps so agents can reason about the algorithm shape. Signals: literal dispatch chain without match/case.
     | Signals: agent-coding-quality/control-flow.literal-dispatch-chain

[PY-AGENT-R009] Info: Function hides algorithm behind nested control flow
  --> $TEMP/quality.py:25:1
  25 | def traverse(groups: list[list[int]]) -> int:
     | `- make this algorithm shape explicit
     | Required: Flatten deeply nested if/loop logic into guard clauses, explicit dispatch, match/case, or small named pipeline steps so agents can reason about the algorithm shape. Signals: nested loops mixed with branches.
     | Signals: agent-coding-quality/control-flow.traversal-knot

[PY-AGENT-R010] Info: Function owns a broad linear algorithm surface
  --> $TEMP/quality.py:40:1
  40 | def summarize(value: int) -> int:
     | `- split this broad function into named algorithm steps
     | Required: Split long public functions with broad linear statement blocks into small named helpers or pipeline steps so agents can edit one algorithm responsibility at a time. Signals: large linear statement block.
     | Signals: agent-coding-quality/control-flow.broad-linear-phase

[PY-AGENT-R011] Info: Function manually spells a native Python transform
  --> $TEMP/quality.py:75:1
  75 | def has_admin(values: list[str]) -> bool:
     | `- replace this boilerplate loop with a native Python idiom
     | Required: Use comprehensions, generator expressions, built-ins such as sum/any/all, collections.Counter/defaultdict, or named iterator pipeline helpers when a loop only maps, filters, counts, groups, accumulates, or answers a predicate. Signals: manual collection accumulator loop, manual predicate loop, manual mapping counter loop, manual mapping grouping loop, manual numeric sum loop.
     | Signals: agent-coding-quality/native-idiom.manual-transform-loop
